<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.cnic.trackrecord.dao.UserDao">
    <sql id="baseColumnFields">
        uid, `account`, `name`, gender, birthday, email, organization, country, province, city, county,
        township, add_time, login_time, role, visit_count
    </sql>
    <select id="getByAccountAndPassword" resultType="User">
        SELECT <include refid="baseColumnFields"/>  FROM `user` WHERE account=#{account} AND password=#{password}
    </select>
</mapper>
